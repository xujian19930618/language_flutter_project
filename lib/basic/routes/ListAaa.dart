
import 'package:flutter/cupertino.dart';

class ListAaa extends StatelessWidget {
  const ListAaa({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
        child: Center(child: Text('底部区域'))
    );
  }

}